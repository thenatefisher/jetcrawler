class Airframe < ActiveRecord::Base
  attr_accessible :make, :model_name, :registration, :serial, :tc, :tt, :year
  
  def load_faa(path)
  
    # make sure all required files exist
    
	# read airframes file
	#  only turboprops and jets
	#  over 12,500
	#  non experimental 

	# check if airframe exists in translation
	#  if not, add to translation table
	  
  end
  
  
end
